# Molejo cluster chart

This immutable release chart contains the Molejo CRDs, Platform Operator, and
outbound Cluster Agent. The local release tool renders the canonical Kustomize
manifests into the packaged chart and replaces image placeholders with published
OCI digests.
