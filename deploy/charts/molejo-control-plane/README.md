# Molejo control plane chart

This chart is an experimental distribution artifact. It requires externally
managed database and bootstrap Secrets and is not part of the alpha installation
acceptance. The local release tool renders the canonical Kustomize manifests
into the packaged chart and replaces image placeholders with published OCI
digests.
