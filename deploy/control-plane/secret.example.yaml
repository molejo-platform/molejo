apiVersion: v1
kind: Secret
metadata:
  name: fruto-control-plane-db
  namespace: fruto-control-plane
type: Opaque
stringData:
  database-url: postgresql://fruto:CHANGE_ME@postgres.example.invalid:5432/fruto
# Apply a real Secret out of band. Never commit this example with credentials.
